# Audit Backlog

_Сгенерировано audit-project (последнее обновление: post-refactor pass 2026-05-19). **Файл перезаписывается при каждом прогоне аудита** — не редактируй вручную._

## Сводка
- Вердикт аудита: **yellow → close to green** (Phase 3 yellow + все namebrand pending пункты теперь закрыты)
- Фазы завершены: Phase 1 Pass 1, Phase 1 Pass 2, Phase 2 (Opus deep), Phase 3 (Opus verification), Backlog drain pass, **Post-refactor pass (QueueController + SessionStore split + version catalog + M1 fix)**
- Тесты: **170 PASSED, 0 SKIPPED, 0 FAILED** across :core test suites (включая un-disabled T1 и 20 новых merge тестов в :core)
- Линтер: не сконфигурирован
- Build verification: `:core` compile + test зелёные; `:cli` compile зелёный; `:app:compileDebugKotlin` configure OK, full compile нужен online (Compose BOM 2026.04.00 не в offline cache)

## MEDIUM (pending)

(нет — M1 закрыт post-refactor pass через accumulate-then-restore в `QueueController.dispatchQueuedItems`)

## LOW (отложено)

- [ ] **`androidx.security:security-crypto` migration.** `1.1.0-alpha07` deprecated by Google; no further releases. Migrate `PairingRepository` + `EncryptedQueueStore` + `AppMasterKey` to DataStore + Tink direct. Multi-day architectural change — оставлено как отдельная задача.

- [ ] **Repository `@Volatile` singleton boilerplate.** 5 repos в `app/src/.../data/` share identical DCL pattern. Extract helper или DI. Cosmetic — не критично.

- [ ] **`ConnectionManagerTest.drainExpiredQueueEntries`** test. Requires DI refactor: `ConnectionManager` currently constructs `PairingRepository` + `EncryptedQueueStore` internally via static `get(context)`. Make them constructor params для testability. ~2 hours.

## Closed in post-refactor pass (2026-05-19)

- **M1 — `dispatchQueuedItems` FIFO under concurrent re-enqueues**: closed via `QueueController` extraction + accumulate-then-restore coordinator. T1 un-disabled and passes.
- **RemoteClient.kt size threshold**: 1148 → 749 LOC. Queue machinery extracted to `QueueController.kt` (407 LOC). Threshold 600 — slight overage in RemoteClient.kt но 35% reduction, residual is load-bearing handshake/resilience KDoc.
- **SessionStore.kt size threshold**: deleted. Split into `SessionListStore.kt` (437 LOC) + `SessionDetailStore.kt` (564 LOC) + `LastSeenIndex.kt` (70 LOC). All под 600 LOC.
- **applyAppendedPlaceholder / reconcileOptimistic pure-fn extraction**: moved to `core/SessionEntryMerge.kt` (122 LOC) as pure functions. 20 new JVM unit tests в `SessionEntryMergeTest.kt`.
- **`SessionStoreTest` meaningless-test pattern**: deleted; replaced with `SessionEntryMergeTest` (covers same logic via the now-tested pure fn `parseExpiredSendMessage` + others).
- **`gradle/libs.versions.toml` version catalog**: adopted. 16 versions, 21 libraries, 4 plugins. All 4 build.gradle.kts files reference catalog. Kept inline: `kotlin("test")` (multi-target shorthand), `project(":core")` (project dep), `application` plugin (Gradle built-in).
- **`:cli:compileKotlin` regression**: fixed (`import kotlinx.coroutines.flow.collect` was wrongly dropped by Phase 2 :cli agent; `Flow<*>.collect()` no-arg still requires the extension import in coroutines 1.11).

## Pre-commit verification

Перед merge online build обязателен:
1. `./gradlew :app:compileDebugKotlin` — Compose BOM 2026.04.00 из Maven (offline cache не содержит)
2. Опц.: `./gradlew :app:assembleDebug` — полная APK сборка с R8

`./gradlew :core:test` + `:cli:compileKotlin` уже offline-verified: 170 PASSED, 0 FAILED, 0 SKIPPED.
